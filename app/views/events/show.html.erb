<h1>Event: <%= @event.name %></h1>

<p>
  <%= link_to 'Edit', edit_location_event_path %> | <%= link_to 'Delete', location_event_path(@location), method: :delete, data: {confirm: "Are you sure you want to delete the event '#{@event.name}'? This cannot be undone!"} %>
</p>

<%= render @event %>
